<%= render 'pages/home' %>

<% if !user_signed_in? %>
<center><%= image_tag("https://s3.amazonaws.com/failboard/failboard.png") %></center>
<% end %>

<div id="fails">
  <%= render @fails %>
</div>

<%= paginate @fails %>