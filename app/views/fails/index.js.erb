$boxes = $('<%= j render(@fails) %>')
$('#fails').append( $boxes ).imagesLoaded( function(){
         $('#fails').masonry( 'reload');
});
<% if @fails.next_page %>
		$('.pagination').replaceWith('<%= j will_paginate(@fails) %>');
<% else %>
	$('.pagination').remove();
<% end %>